function asec(x, y) {
    return x.value - y.value
}

function dsec(x, y) {
    return y.value - xy.value
}

function max(x, y) {
    return x.value > y.value ? x.value : y.value
}

function min(x, y) {
    return x.value < y.value ? x.value : y.value
}